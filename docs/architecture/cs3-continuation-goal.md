---
artifact_contract: ce-handoff/v1
created_at: "2026-09-11T16:44:32Z"
title: "CS3 continuation: provisional integration through U10"
summary: "Resume CS3 from committed C5 while AR1 owns appearance research; preserve technical and release gates."
repository: "ridermw/midcreek-cs-3"
branch: "main"
resume_focus: "U6 guarded loading, U5 technical packaging and the U7 playable, followed by U8-U10."
keywords: ["CS3", "C5", "U6", "AR1", "paused", "provisional"]
---

# Goal for a brand-new CS3 session

## Copyable objective

Resume Mid Creek CS3 in `ridermw/midcreek-cs-3` on `main`, using the committed
plan and this handoff. Finish the remaining U5 technical work and implement
U6-U10 against an explicitly recorded, frozen provisional development baseline.
Do not duplicate the appearance research now owned by AR1 in
`williamsmat_microsoft/midcreek-concept`.

Read `plan.md`'s September 11 continuation section, then the Goal Capsule,
continuation boundary, Verification Contract and Definition of Done in
`docs/architecture/cs3-blueprint.md`. Work the remaining units in dependency
order, reading each unit and its cited contracts before editing. README and
TODOS contain current status; earlier research sections and old session
summaries are historical, not evidence that implementation never started.
`plan.md` is the only active work plan. `docs/archive/` contains retired plans
and old handoffs, not alternative goals. Retained research documents and U1-U4
contract sections are explicitly reference-only; do not execute their old
next-step instructions or reimplement completed work.

Continue autonomously within the actual remaining authorization once the user
starts this new session's goal. Record progress in coherent reviewed commits,
receipts and the existing monitor rather than routine approval prompts. Finish
with exact implemented/qualified/blocked statuses. Do not claim full U10,
appearance acceptance or release readiness when an applicable gate is unmet.

## Starting state

- U1-U4 are complete: pinned toolchain/entries, deterministic simulation and
  layout, fixed-tick input/replay/camera, and immutable approved references.
- U5 is incomplete, with a working native Blender-to-GLB-to-Three.js technical
  prototype. No production asset library has been promoted.
- C5 source and tests are committed at
  `9844435f98848be030a3347108e1129c650ed875`. This includes the second reflective
  band, orange piping, shaped calves/boots and differentiated leather equipment.
  C4's diagnostic Repair task, rigid hierarchy and clip contracts are preserved.
- Recorded C5 evidence includes four atlas tests, ten native Blender tests,
  31 export/browser checks, 30 matched contacts, 80 isolated views and 40 hall
  views. Its 211 catalog images, including the atlas, were receipt-matched.
  A bounded static source review found no supported correctness blocker.
- The last independent overall appearance review assessed the EARLIER C3
  candidate and returned needs-work. C4/C5 corrections have not been accepted
  by an independent overall appearance review.
- U6-U10 have not been implemented. The application entries remain shells,
  not a playable game or a qualified populated showcase.
- The user requested a pause and a clean committed/pushed handoff. The old
  session's live monitor and keep-awake support are stopped at handoff. Do not
  treat old process handles or a displayed monitor URL as a running service.

The user reports that AR1 has started in another session. This is not evidence
that AR1 has finished, delivered qualified assets, or expanded CS3 permissions.
Keep the concept repository read-only and coordinate before shared-machine
Blender/GPU/browser work. Do not stop or manage the other session's processes.

## First bounded actions

1. Verify branch/history, worktree state, the C5 source commit, authorization
   identity, remaining time/disk allowance and absence of another active CS3
   integration owner. Do not repeat R1-R3 or ask for the already-satisfied
   original launch approval.
2. Locate and check the retained C5 authoring/export/review receipts. Compare
   their input hashes to the committed source. Do not rerun completed studies
   merely because this is a new session.
3. Resolve the proposed provisional-use boundary explicitly. Before a real
   application consumes appearance-unaccepted assets, record the actual
   authorizing user instruction, exact source/library/profile hashes, allowed
   development uses and remaining gates in a separate dated amendment.
   Do not overwrite the immutable parent authorization or fabricate a review.
4. Begin U6 from its already-satisfied U1/U2 implementation dependency. It can
   be implemented before AR1 finishes; real integration uses validated assets
   and the recorded development-use exception, not an assumed U5 pass.

The preceding session recommended provisional integration; it did not enact an
appearance approval, production promotion or public-release exception. If the
user in the new session has not authorized provisional use, proceed with dependency-independent
U6 implementation and leave that integration gate explicit rather than silently
widening the original policy.

## Remaining implementation work

### U6: guarded loading and resource ownership

Implement the existing manifest/library/instance/lifecycle contracts, not a
second viewer or a second simulation. Cover strict manifest and asset identity,
the 30,000 ms required-load deadline, partial failure cleanup, late completions,
application-generation guards and context loss during both loading and ready.
Errors disable gameplay and expose Reload; context restoration must not revive
an old application session.

Required-ready includes validated attachment, time-zero posing, a nonempty
production render, the prescribed one-time GPU completion fence, enabled
HUD/input, the following RAF boundary and a final generation/context check.
Library resources are shared; disposing one instance must not break another.
Dispose unique shared resources and ImageBitmaps exactly once at library
teardown. Use real browser failure/ownership tests as required by U6.

### U5 technical closure, without another art campaign

Keep the C5 visual source frozen unless a technical defect requires a bounded
correction. Complete production manifest generation, the remaining negative
cases, failure-preserving atomic publication mechanics and the prescribed two
fresh technical repetitions after the development recipe is fixed.

Preserve the actual source/geometry/UV/decoded-texture/pose comparisons and
the original eight-node R2 regression. Do not weaken a checker to admit the
prototype. Private cel/ink studies are not a selected production recipe; follow
the blueprint's initial normal-rendering configuration and record any intentional
source/browser adaptation.

Keep the explicitly authorized development-library selection distinct from
the qualified production pointer and release allowlist. Default promotion must
still reject absent appearance/policy approval. No generated concept image or
AI-retouched screenshot can substitute for a genuine source/browser capture.

### U7: the actual playable

Connect the real simulation, renderer, library, HUD, input and animation.
Preserve the fixed 17-by-15 hall, 32 racks, 37 placements, seed, tick timing,
dispatch/cancellation/arrival/repair behavior and camera rules. Use the five
existing asset IDs and the declared Idle/Walk/Repair clips.

Exercise the full browser journey and the prescribed animation, pause/hidden,
restart, projection and occlusion cases. Preserve native rack occlusion.
No x-ray character, rack fading, root-motion gameplay, fallback missing clips
or second animation-owned repair timer.

### U8-U10: measure, stage and enforce

Implement complete readiness, request and all-pass frame ledgers before making
performance claims. Run the three prescribed named-target repetitions under
the actual foreground/display/browser/GPU conditions, not a convenient proxy.
Report passed, failed and unqualified separately. Activate D2 only after a
measured normal-rendering miss; do not relax appearance or performance budgets.

Build U9's isolated showcase/gallery and U10's release allowlist, prefixed
traversal/privacy rejection and CI controls. Respect each member's actual
publication permission. Implement and test release controls even if a final
release prerequisite remains blocked; do not call that a qualified release.
Do not deploy Pages.

All measurements identify the exact frozen asset and rendering recipe. A later
AR1 library, texture, material, animation or shader change invalidates affected
claims and requires the corresponding checks again.

## AR1 boundary and replacement

AR1 owns improved prompts, coherent production references and the authoring/
rendering investigation. No further broad appearance sweeps belong to this CS3
continuation. The first meaningful product target is the playable hall.

Do not watch or import a moving concept-repository tip. Accept only a versioned
handoff with source/asset hashes, provenance, appearance verdicts, rendering
requirements and measured cost differences. Obtain any required new-input/use
amendment before copying or publishing changed external inputs.

If no qualified AR1 replacement is available within the valid allowance, keep
the provisional baseline and report the remaining appearance/release dependency.
Do not wait indefinitely, quietly renew the run or relabel unresolved work as
finished. Final human appearance/release review remains separate.

## Authorization and operating rules

The parent record is
`.artifacts/implementation/20260910T232859Z/authorization.json`.
Its SHA-256 is
`1d6bf6ccceb960428296591f41fe0c35271ecf668c5959dd27dd9e503b593624`.
The R3 revision is `732ff3936f83a0de87547f567e93789f1f11001d`;
the approved concept reference pin is
`870603632c4b6665c513d0fa692a3ee2dae2b683`.

The ORIGINAL heavy-work deadline is September 12, 2026 at
22:28:59.608228 UTC; closeout ends at 23:28:59.608228 UTC.
A fresh session or pause does not reset these clocks. If they have expired,
do not launch jobs using this allowance; obtain an explicit new authorization.
Missing local authorization is likewise not permission to reconstruct one
from this public summary.

The original disk limits, closeout reserve and one-heavy-job rule remain.
The user waived the assistant-credit ceiling, not deadlines, resource limits,
new paid services or source/publication restrictions. Use `tools/run_guard.py`
with unique finite job names/timeouts for heavy work and retain failed receipts.
Do not add image-generation calls to CS3 because AR1 has separate permission.

Use one integration owner; no factories, recursive delegation, new gameplay,
D1, D3, Rust/Bevy, unapproved sibling writes or global GitHub-account switching.
Commit and push coherent reviewed increments to `main` comfortably below the
one-hour active-work ceiling. No empty/WIP commits, force pushes, evidence
deletion or passing labels for failed work.

Start only support processes needed by the resumed work, record ownership,
check that they are responsive, and stop them on pause/closeout. Preserve the
user's existing applications and other sessions' work.

## Evidence and portability

Generated evidence remains ignored and LOCAL; it was not swept into Git by the
request to commit all source/documentation work:

- `.artifacts/assets/u5-c5/authoring.json`: exact influencing input hashes.
- `.artifacts/assets/u5-c5/library.blend`: current candidate, SHA-256
  `5b9870942fb42a1390aa048c6840e99ab3dafbc94ea63fe4c75e44563b8fd2da`.
- `.artifacts/assets/u5-c5/export/checks-c5.json`: technical checks and contacts.
- `.artifacts/implementation/20260910T232859Z/u5-appearance-c3-fine.json`:
  the last independent overall appearance verdict.
- The same run root's `u5-cel-study-c5/` and `u5-hall-study-c5/`:
  private studies, not a qualified rendering recipe.
- `checkpoint-U5-character.json` and `checkpoint-PAUSE-CS3-handoff.json`:
  committed source checkpoint, pause state and final delivery identities.
- `u5-monitor/index.html`: retained offline HTML snapshot; image paths depend
  on local files. `u5-monitor-status.json` supplies authored status.

On the same machine, verify and reuse those files. A clean clone contains the
source and this goal, not ignored reference masters, candidate binaries or
operational receipts. If local evidence is unavailable, obtain the authorized
handoff or regenerate from pinned inputs under a valid allowance with new
identities; do not claim that a regenerated binary is the old captured source.

At final closeout, state the exact commit/library/profile, implemented units,
technical/appearance/performance/publication results, remaining human decisions
and stopped owned processes. A partial or blocked outcome is preferable to a
false full-qualification claim.