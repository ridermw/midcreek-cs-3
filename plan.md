# Mid Creek CS3 - Active Continuation Plan

**This is the only active work plan.** Execution is paused for a clean,
committed handoff to a brand-new session. Start that session with
[the committed goal](docs/architecture/cs3-continuation-goal.md).

The [blueprint](docs/architecture/cs3-blueprint.md) is the detailed implementation
and verification contract, not an instruction to restart completed units.
[README.md](README.md) states current status; [TODOS.md](TODOS.md) lists only
remaining/deferred work. Documents under [docs/archive/](docs/archive/README.md)
are historical and must not be selected as goals.

## Already completed - do not repeat

- R1 research, R2 technical proof, R3 review and the original launch preflight.
- U1 tooling/entry builds, U2 simulation/layout, U3 input/replay/camera and U4
  reference import.
- C5 character source corrections and tests, committed at
  `9844435f98848be030a3347108e1129c650ed875`.
- The local HTML U5 monitor and the recorded source/export/browser studies.

Keep implemented code and regression tests intact. Read old work only when a
remaining unit cites it as evidence or prior art. An older handoff saying
"implementation has not started" or "C5 is uncommitted" is superseded.

## September 11 pause and continuation plan

The user paused this session on September 11, 2026 at 16:36:56 UTC and required
all source/documentation work committed and pushed before handoff. Do not
restart implementation or support processes in this session.

The next CS3 session owns technical integration and the playable. The user
reports that AR1 owns appearance-authoring/rendering work in a separate
`williamsmat_microsoft/midcreek-concept` session. Keep that repository read-only;
do not duplicate its art experiments or interfere with its processes.

### 1. Verify the committed starting point

Check `main`, current worktree/remote state, the C5 source commit and retained
authoring-input hashes. Verify the actual authorization, remaining allowance
and absence of another active CS3 integration owner. Do not rerun completed
image studies merely because the session is new.

Private references, generated candidate binaries/images and operational
receipts remain ignored. They are not publication-approved by a request to
commit source work. The goal document identifies the retained evidence and
what a clean clone does not contain.

### 2. Implement U6 now

U6 implementation depends on already-complete U1/U2 and need not wait for AR1.
Implement guarded asset loading, manifest validation, the required-ready
boundary, visible Reload failures, context-loss/generation handling, partial/
late-load cleanup and shared library/instance ownership.

Use U6's existing blueprint scenarios and real browser evidence, including
two instances and disposal/context-loss failures. No second simulation/viewer,
silent fallbacks or proxy first-frame readiness.

### 3. Finish U5 technical packaging for the frozen baseline

C5 is a technical prototype, **not an appearance-accepted or promoted library**.
Complete manifest/export packaging, remaining negative cases, prescribed
fresh repetitions after fixing the development recipe, and failure-preserving
publication mechanics. Keep all source/geometry/UV/texture/clip checks and
the original R2 regression.

Before real development integration uses unaccepted art, record the actual
provisional-use authorization, exact candidate/library/profile hashes, allowed
uses and remaining gates in a dated amendment. Do not rewrite the immutable
launch record. If that permission is not yet supplied, continue independent
U6 implementation without pretending the integration gate is open.

Keep development selection separate from the qualified production pointer
and release allowlist. Default promotion must reject missing appearance or
policy approval. This continuation plan does not itself grant an appearance
pass or an already-issued promotion exception.

### 4. Build U7's actual playable

Wire the validated library, one simulation, scene, HUD, input and three clips.
Preserve layout, timing, movement/dispatch/repair semantics and native rack
occlusion. Exercise the complete browser journey and U7's animation,
pause/hidden/restart/projection/occlusion cases.

The next major visible result is a playable hall using the recorded development
baseline, not another aesthetic image sweep.

### 5. Implement and qualify U8-U10

Implement U8 readiness/request/all-pass frame ledgers before measurements.
Use the exact named-target conditions and three required repetitions; report
passed, failed and unqualified separately. Activate D2 only after a measured
normal-rendering miss.

Build U9's isolated showcase/gallery and U10's release-content controls.
Their code and tests may advance while a production prerequisite remains
blocked, but that is not full production or release qualification. Enforce
each member's publication permission; no Pages deployment.

Every claim identifies the actual frozen asset/rendering configuration.
If AR1 remains unavailable at closeout, report that appearance/release
dependency rather than claiming full U5/U10 completion.

### 6. Replace art only through a controlled handoff

Consume a versioned AR1 delivery with source/bundle hashes, provenance,
appearance verdicts, renderer requirements and cost differences. Obtain any
required changed-input/use approval before importing or publishing it.
Do not chase a moving sibling tip during qualification.

Rerun affected source, integration, performance and publication gates after
replacement. Keep final human appearance/release review separate.

## Operating boundaries

- Authority: `.artifacts/implementation/20260910T232859Z/authorization.json`.
  R3 and original launch approval are satisfied; do not restart their workflow.
- Original heavy-work deadline: **September 12, 2026 at 22:28:59.608228 UTC**.
  Original closeout deadline: **23:28:59.608228 UTC** that day. Pause or a new
  session does not renew either. Expired/missing authorization requires an
  explicit new grant, not a reconstructed record.
- The recorded credit-ceiling waiver does not waive disk/reserve limits,
  one-heavy-job ownership, new-service restrictions or publication rules.
- One integration owner; all sibling repositories read-only. Coordinate
  shared-machine heavy work with AR1 without managing its processes.
- No D1, D3, new gameplay, factories, recursive delegation, Rust/Bevy or Pages.
- Commit reviewed, verified increments to `main` frequently, comfortably below
  the one-hour active-work ceiling. No WIP/empty commits or force pushes.
- Use the existing resource guard and retain failed evidence. Stop owned
  processes on pause/closeout; do not leave unverified work described as done.
